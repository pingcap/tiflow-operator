---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: tiflowclusters.pingcap.com.pingcap.com
spec:
  group: pingcap.com.pingcap.com
  names:
    kind: TiflowCluster
    listKind: TiflowClusterList
    plural: tiflowclusters
    singular: tiflowcluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TiflowCluster is the Schema for the tiflowclusters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TiflowClusterSpec defines the desired state of TiflowCluster
            properties:
              configUpdateStrategy:
                type: string
              executor:
                description: TiflowExecutor defines the desired state of tiflow executor
                properties:
                  baseImage:
                    type: string
                  config:
                    items:
                      type: string
                    type: array
                  pvReclaimPolicy:
                    type: string
                  replicas:
                    type: integer
                  requests:
                    items:
                      type: string
                    type: array
                  stateful:
                    type: boolean
                required:
                - baseImage
                - config
                - pvReclaimPolicy
                - replicas
                - requests
                - stateful
                type: object
              master:
                description: TiflowMaster defines the desired state of tiflow master
                properties:
                  baseImage:
                    type: string
                  config:
                    items:
                      type: string
                    type: array
                  maxFailoverCount:
                    type: integer
                  replicas:
                    type: integer
                required:
                - baseImage
                - config
                - maxFailoverCount
                - replicas
                type: object
              version:
                type: string
            required:
            - configUpdateStrategy
            - executor
            - master
            - version
            type: object
          status:
            description: TiflowClusterStatus defines the observed state of TiflowCluster
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
